--Constructor Enum

Constructor Enum khác so với Constructor của đối tượng bình thường vì: 
-Các giá trị hằng số trong enum được khai báo báo từ trước và được quản lý bởi trình biên dịch.

-Và các giá trị này không được thay đổi cũng như thêm mới nên không thể sử dụng từ khóa new để khởi tạo một đối
tượng mới như constructor bình thường.

-Constructor của enum chỉ được sử dụng để khởi tạo giá trị cho các hằng số trong enum 
và nó được gọi một cách tự động khi mỗi hằng số được khai báo.

-Vì enum chỉ có thể có các hằng số cố định và không thể tạo ra thêm hằng số mới, 
nên constructor của enum không thể được gọi để tạo ra các đối tượng mới.

-constructor của enum không thể được gọi bằng cách sử dụng từ khóa `new`, 
không thể được kế thừa hoặc triển khai bởi các lớp con, và không thể được gọi từ bên ngoài enum.

-Các giá trị của hằng số trong enum nên để là final 

-Có thể khởi tạo nhiều constructor cho một enum và khi tạo ra một hằng số enum thì có thể khởi tạo tùy biến