Có một số lớp cung cấp các cài đặt của các giao diện bản đồ (map interfaces). 
Các lớp có thể được sử dụng cho các bản đồ được tóm tắt ở đây:

AbstractMap:    : Triển khai hầu hết các phương thức của Map, (có thể gây nhầm lẫn ở câu này)
                  lớp này có thể hiểu là nó triển khai tất cả các phương thức của Map vì đây là
                  kiến thức cơ bản của một Interface, các lớp thực hiện(implements) inteface đó 
                  bắt buộc phải thực hiện tất cả phương thức của Interface đó

EnumMap         : kế thừa AbstractMap để sử dụng với các khóa enum

HashMap         : kế thừa AbstractMap để sử dụng các phương thức cơ bản, và sử dụng cấu
                  trúc dữ liệu bảng băm

TreeMap         : kế thừa AbstractMap để sử dụng các phương thức cơ bản, và sử dụng cấu
                  trúc dữ liệu Cây

WeakHashMap     : kế thừa AbstractMap để sử dụng bảng băm với các khóa yếu (weak keys)

LinkedHashMap   : Mở rộng HashMap để cho phép lặp theo thứ tự chèn vào

IdentifyHashMap : Mở rộng AbstractMap và sử dụng bằng cách so sánh tính toàn vẹn tham chiếu khi so sánh tài liệu
