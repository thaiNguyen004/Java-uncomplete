• Lớp AbstractMap trong Java là một lớp trừu tượng cung cấp một số triển khai mặc định cho giao diện Map. 
Lớp này cung cấp một số phương thức cơ bản của Map và để lại một số phương thức trừu tượng 
để được triển khai bởi các lớp con.
• Có thể dùng lớp AbstractMap để thực hiện các phương thức của giao diện Map nếu bạn muốn triển khai một 
lớp Map tuỳ chỉnh của riêng mình. Bằng cách kế thừa từ lớp AbstractMap, bạn chỉ cần triển khai các phương 
thức trừu tượng còn lại mà không cần phải triển khai lại các phương thức đã được cung cấp bởi AbstractMap.

• Lớp AbstractMap cung cấp một số triển khai mặc định cho các phương thức của giao diện Map, bao gồm:

1. put(K key, V value): Thêm một cặp key-value vào Map.
2. get(Object key): Lấy giá trị tương ứng với key từ Map.
3. remove(Object key): Xóa một entry từ Map dựa trên key.
4. containsKey(Object key): Kiểm tra xem key có tồn tại trong Map hay không.
5. containsValue(Object value): Kiểm tra xem value có tồn tại trong Map hay không.
6. isEmpty(): Kiểm tra xem Map có rỗng hay không.
7. size(): Trả về số lượng các entry trong Map.
8. clear(): Xóa toàn bộ các entry trong Map.
• Lớp AbstractMap để lại một số phương thức trừu tượng để được triển khai bởi các lớp con. Các phương thức này bao gồm:

1. entrySet(): Trả về một tập hợp (Set) các entry trong Map.
2. keySet(): Trả về một tập hợp (Set) các key trong Map.
3. values(): Trả về một Collection chứa tất cả các giá trị trong Map.